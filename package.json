{
  "name": "codex-theme",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -ws --if-present run build",
    "clean": "npm -ws --if-present run clean",
    "lint": "npm -ws --if-present run lint"
  },
  "devDependencies": {
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  }
}
